# Phase 1 Setup Commands

Run these from the project root after initializing Node.js:

```bash
npm init -y
npm install @openclaw/sdk csv-parse zod
npm install -D typescript tsx @types/node
```

Optional but commonly useful for OpenClaw workflows that call APIs:

```bash
npm install axios
```
